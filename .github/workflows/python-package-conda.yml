名称: 使用 Conda 的 Python Package[推]工作:Build-linux:运行: ubuntu-latest策略:最大平行度: 5步骤:- 用途: actions/checkout@v2- name: Set up Python 3.10Uses: actions/setup-python@v2与... 有关:Python 版本: 3.10- name: 将 conda 添加到系统路径快跑# $CONDA 是一个指向 miniconda 目录根目录的环境变量$CONDA/bin > > $GITHUB _ path- name: 安装依赖项快跑Conda env update -- file environment.yml -- name base名字: 皮棉快跑Conda install flake8如果有 Python 语法错误或未定义的名称，停止构建Flake8. -- count -- select = E9，F63，F7，F82 -- show-source -- statistics# exit-zero 将所有错误视为警告Flake8. -- count -- exit-zero -- max-complexity = 10 -- max-line-length = 127 -- statistics- 名称: 用 pytest 进行测试快跑连续安装 pytest化脓试验
